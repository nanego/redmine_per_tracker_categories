<%= f.select :category_id, (@issue.project.issue_categories.reject{|c| c.trackers.present? && c.trackers.exclude?(@issue.tracker) }.collect {|c| [c.name, c.id]}), :include_blank => true, :required => @issue.required_attribute?('category_id') %>
